- The task system that I am creating will let clients create, update, delete, and view tasks as well as organize the tasks and link them to specific users. It does this by providing a RESTful API. The concepts will be accessed using HTTP methods/JSON representations and will be treated as resources. The API is stateless, easily evolvable, and consistent

- Frontend applications, developers, or automated clients

- users, tasks, lists